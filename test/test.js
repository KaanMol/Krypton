var varRWord,
    varUWord;



for (var userWord = 0; userWord < 6; userWord++) {
  for (var randomWord = 0; randomWord < 6; randomWord++) {
    if (varRWord.charAt(userWord) == varUWord.charAt(randomWord)) {
      console.log('jup')
    }
  }
}
